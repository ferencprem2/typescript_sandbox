Download tsc: `npm install -g typesctipt`
Version: `tsc -v`
Create package.json: `npm init -y`
Add tsc to dependencies: `npm install typescript --save-dev`
Initialize tsc, create tsconfig.json: `tsc --init`